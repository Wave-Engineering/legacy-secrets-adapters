# Database configuration — rendered by the broker sidecar.
# The legacy app reads this file; the broker keeps it fresh.
[database]
host = {{ host }}
port = {{ port }}
dbname = {{ dbname }}
username = {{ username }}
password = {{ password }}
